<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - Painel</title>
</head>
<body>
    <h1>Dashboard</h1>
    <p>Olá, <%= user.username %>!</p>
    
    <h2>Estatísticas do Bot</h2>
    <% if (stats && !stats.error) { %>
        <ul>
            <li>Servidores: <%= stats.guilds %></li>
            <li>Usuários: <%= stats.users %></li>
            <li>Canais: <%= stats.channels %></li>
        </ul>
    <% } else { %>
        <p>Não foi possível carregar as estatísticas do bot. Verifique se ele está online.</p>
    <% } %>

    <a href="/auth/logout">Sair</a>
</body>
</html>
